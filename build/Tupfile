include_rules

CFLAGS += -D DOCTEST_CONFIG_DISABLE

: foreach ../src/*.cc ^../src/test.cc |> !cc |>
: ../external/loguru/loguru.cpp |> !cc |>
: *.o ../external/lua/liblua.a |> !ld |> nebula

.gitignore
